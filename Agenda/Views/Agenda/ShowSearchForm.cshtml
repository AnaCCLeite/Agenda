@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}
<div class="row">
    <div class="col-md-4">
        <form asp-action="ShowSearchResults">
            <div class="form-group">
                <label asp-for="TermoDeBusca" class="control-label"></label>
                <input asp-for="TermodeBusca" class="form-control" />
            <</div>
             <div class="form-group">
                <input type="submit" value="Busca" />
            </div>
        </form>
    </div>
</div>

